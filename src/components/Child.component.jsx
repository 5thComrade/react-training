// eslint-disable-next-line react/prop-types
const Child = ({ name }) => {
    return (
      <section>
        <h1>This is the Child here! {name}</h1>
      </section>
    );
  };
  
  export default Child;
  